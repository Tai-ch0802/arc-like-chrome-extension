# 開發摘要 - 2026-02-26

## 變更項目
- [`web/guide.html`]: 新增完整的擴充功能使用指南頁面
- [`web/css/style.css`]: 針對導覽列實作全新 RWD 設計，預設只顯示 Icon 並且加入 Hover 時展開文字的過場動畫，解決 1150px 以下小螢幕的擠壓問題
- [`web/locales/*.json`]: 新增並翻譯共 124 筆多國語系字串，包含預設英文以及涵蓋日文、泰文等 12 國語言

## 技術決策
- 放棄依賴 `max-width` 來隱藏文字元素，改為全域隱藏導覽列文字並利用 css transitions 達成 hover 時文字滑出（slide-out）的動畫，這讓跨裝置的設計更有一致性且介面更加簡潔
- 透過 AI Agent 自製 python 與 Node.js script 抓取 html 中帶有 `data-i18n` 且不存在於 `en.json` 的 key 並使用 Google API 批次將缺失語言全數自動補齊

## 待辦事項
- [ ] 無
