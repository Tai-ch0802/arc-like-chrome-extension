# 開發摘要 - 2026-02-27

## 變更項目

### 戰略二：Opinionated Design (CSS 視覺升級)
- [sidepanel.css]: 新增 Design Token 系統 (`--arc-*` CSS variables)，包含 Animation/Shadows/Glassmorphism/Hover tokens
- [sidepanel.css]: Tab hover 微動畫 (translateX + box-shadow transition)
- [sidepanel.css]: SortableJS 拖曳打磨 (sortable-chosen scale+shadow, sortable-ghost dashed border, drop-target pulse)
- [sidepanel.css]: Toast glassmorphism (backdrop-filter + glass variables)
- [sidepanel.css]: `@media (prefers-reduced-motion)` 全域覆寫
- [sidepanel.css]: Glassmorphism `@supports` fallback

### 戰略三：Hover Summarize (AI 懸浮摘要)
- [modules/ui/hoverTooltip.js]: 🆕 Tooltip UI 元件 (shimmer/streaming/glassmorphism/mouseenter-to-keep-visible)
- [modules/ui/hoverSummarizeManager.js]: 🆕 核心邏輯 (2s debounce, AbortController, chrome.scripting 文字擷取, 記憶體快取, URL 變化清快取)
- [modules/aiManager.js]: 新增 Summarizer API 封裝 (checkSummarizerReadiness, getOrCreateSummarizerSession, destroySummarizerSession)
- [modules/stateManager.js]: 新增 Hover Summarize toggle state (initHoverSummarize, isHoverSummarizeEnabled, setHoverSummarizeEnabled)
- [manifest.json]: 新增 `"scripting"` permission
- [sidepanel.js]: 整合 import + init + cache hooks (onRemoved/onUpdated)
- [modules/uiManager.js]: 新增 re-export
- [sidepanel.css]: Hover Tooltip CSS (glassmorphism + shimmer animation)
- [_locales/*/messages.json]: 14 個 locale 新增 hoverSummarizeToggle / hoverSummarizeDescription

## 技術決策
- Hover Summarize 使用 **Summarizer API** (Chrome 138+) 取代 Prompt API — 專為摘要設計，有原生 type/length/format 參數控制
- 採用 **Lazy Session** 策略 — 使用者首次 Hover 時才建立 Summarizer session，不佔用啟動資源
- 語言輸出依使用者設定語言，不支援的語言回退至英文 (Summarizer API 目前支援 en/es/ja)
- Design Token 使用 `--arc-*` 前綴，與現有主題 CSS 變數共存，逐步遷移

## 待辦事項
- [ ] 在 Chrome 中實際載入測試所有視覺效果
- [ ] 設定面板中新增 Hover Summarize toggle UI（目前只有 state，尚未在 settingManager 中渲染開關）
- [ ] 確認 Summarizer API 在目標 Chrome 版本可用
- [ ] 考慮戰略一（Zero-Config AI 擴展）和戰略四（Market Positioning）的 SA
