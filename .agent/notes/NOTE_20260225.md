# 開發摘要 - 2026-02-25

## 變更項目
- [modules/searchManager.js]: 修正 `updateGroupVisibility` 函式，將 CSS class `collapsed` 操作改為 inline `style.display`，修復搜尋時群組未自動展開的 Bug。
- [modules/searchManager.js]: 新增 `bookmarkSearchGeneration` 計數器，修復清除搜尋後書籤卡在過濾狀態的競速條件。
- [usecase_tests/puppeteer_tests/search_group_expand.test.js]: 新增群組自動展開 E2E 測試 (2 cases)。
- [usecase_tests/puppeteer_tests/search_bookmark_clear.test.js]: 新增書籤搜尋清除 E2E 測試 (3 cases)，含競速條件防護驗證。

## 技術決策
- 群組展開修復：選擇修改 `searchManager.js` 使其與 `tabRenderer.js` 的 inline `style.display` 模式一致。
- 競速條件修復：使用 generation counter 模式，在 `filterBookmarks` 的 async await 之後檢查是否有更新的搜尋，若有則棄置過時結果。

## 待辦事項
- [ ] 未來可考慮統一所有群組展開/收合邏輯到單一模組。
