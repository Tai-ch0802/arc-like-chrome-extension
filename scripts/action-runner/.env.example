# GitHub Actions Self-Hosted Runner - Environment Variables
# Copy this file to .env and fill in the values:
#   cp .env.example .env && nano .env

# [Required] GitHub owner (user or organization)
GITHUB_OWNER=Tai-ch0802

# [Required] GitHub repository name
GITHUB_REPO=arc-like-chrome-extension

# [Required] GitHub Personal Access Token (PAT)
# Required scope: repo (for private repos) or public_repo (for public repos)
# Or Fine-grained PAT with Administration: Read & Write
# Generate at: https://github.com/settings/tokens
GITHUB_TOKEN=ghp_your_token_here

# [Optional] Runner display name (default: vm-runner-<hostname>)
RUNNER_NAME=vm-runner-1

# [Optional] Runner labels, comma-separated (default: self-hosted,linux,x64)
RUNNER_LABELS=self-hosted,linux,x64

# [Optional] Runner work directory (default: _work)
RUNNER_WORKDIR=_work

# [Optional] Runner version (default: 2.331.0)
# RUNNER_VERSION=2.331.0

# [Optional] Runner user (default: runner)
# RUNNER_USER=runner
